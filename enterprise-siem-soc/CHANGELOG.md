# Changelog

Record every change to a live environment here, especially anything tagged
`STATUS: Requires change control` in the docs. Format: date, component, change,
validation result, author.

## Unreleased

- Initial documentation set published (Parts 00–14), covering the validated lab
  build (1 bare-metal host, 1 VM, ~10 agents) and the production scaling plan.
